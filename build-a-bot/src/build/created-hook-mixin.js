export default {
  created() {
    console.log('component created');
  },
};
// reducing duplication with mixins. used here only as to try it out.
// along with components with lifecycle hooks it could be great
